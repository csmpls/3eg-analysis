
subjects = ['nick', 'john']
electrodePositions = ['erb', 'erh', 'normal']

# returns an array of arrays
# each sub-array is a log-binned feature vector
def loadRecordings(subject, electrodePos, task)
 
